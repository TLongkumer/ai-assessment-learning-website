name: Stage assessment site source

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Staging placeholder
        run: echo Source archive parts are being staged.
